// Solution with for loop:

// let number = 12;

// for(let index = 1; index <= number; index ++){
//     if (number % index === 0){
//         console.log(index + ' is divisor of ' + number);
//     }
// }

// Testing with other number: 


let number = 50;

for(let index = 1; index <= number; index ++){
    if (number % index === 0){
        console.log(index + ' is divisor of ' + number);
    }
}